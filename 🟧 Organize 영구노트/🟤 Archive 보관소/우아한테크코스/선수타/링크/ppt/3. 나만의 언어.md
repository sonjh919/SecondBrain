---
cssclasses:
  - wide
---

```dataviewjs
// -- Theme Detection --
function detectIsDarkMode(){
    const body = document.body;
    if(body?.classList?.contains("theme-dark")) return true;
    if(body?.classList?.contains("theme-light")) return false;
    const computedBg = window.getComputedStyle(document.documentElement).backgroundColor || "";
    const ch = computedBg.match(/\d+(?:\.\d+)?/g);
    if(ch && ch.length >= 3){
        const lum = (Number(ch[0])*299 + Number(ch[1])*587 + Number(ch[2])*114)/1000;
        return lum < 128;
    }
    return true;
}

const isDark = detectIsDarkMode();

const t = isDark ? {
    bg: '#08080e',
    text: '#ffffff',
    textMuted: 'rgba(255,255,255,0.35)',
    textSub: 'rgba(255,255,255,0.55)',
    border: 'rgba(255,255,255,0.06)',
    glow: 'rgba(255,255,255,0.03)',
    accent: 'var(--zos-accent, #6366f1)',
    accentGlow: 'rgba(99,102,241,0.15)',
} : {
    bg: '#fafafa',
    text: '#0f0f0f',
    textMuted: 'rgba(0,0,0,0.3)',
    textSub: 'rgba(0,0,0,0.55)',
    border: 'rgba(0,0,0,0.06)',
    glow: 'rgba(0,0,0,0.02)',
    accent: 'var(--zos-accent, #6366f1)',
    accentGlow: 'rgba(99,102,241,0.08)',
};

const container = this.container;
container.style.cssText = 'padding:0;margin:0;';

container.innerHTML = `
<style>
  .lang-stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 82vh;
    padding: 60px 0;
    font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Pretendard', sans-serif;
    position: relative;
    overflow: hidden;
    user-select: none;
  }

  .lang-stage::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 500px;
    height: 500px;
    border-radius: 50%;
    background: ${t.accentGlow};
    filter: blur(120px);
    pointer-events: none;
    z-index: 0;
  }

  .lang-title {
    font-size: clamp(32px, 5vw, 48px);
    font-weight: 200;
    color: ${t.text};
    text-align: center;
    line-height: 1.5;
    letter-spacing: -0.5px;
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: lang-fadeUp 1.2s ease 0.3s forwards;
    margin-bottom: 64px;
  }

  .lang-title em {
    font-style: normal;
    font-weight: 600;
    background: linear-gradient(135deg, ${t.accent}, ${isDark ? '#a78bfa' : '#818cf8'});
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .lang-sections {
    display: flex;
    flex-direction: column;
    gap: 28px;
    position: relative;
    z-index: 1;
  }

  .lang-section {
    display: flex;
    flex-direction: column;
    gap: 12px;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.8s ease, transform 0.8s ease;
    cursor: default;
  }

  .lang-section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .lang-header {
    display: flex;
    align-items: baseline;
    gap: 20px;
  }

  .lang-desc {
    font-size: clamp(17px, 2.5vw, 22px);
    font-weight: 300;
    color: ${t.textMuted};
    line-height: 1.7;
    padding-left: 48px;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }

  .lang-desc.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .lang-badges {
    display: flex;
    gap: 12px;
    padding-left: 48px;
    flex-wrap: wrap;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }

  .lang-badges.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .lang-badge {
    font-size: clamp(17px, 2.5vw, 21px);
    font-weight: 400;
    color: ${t.accent};
    border: 1px solid ${t.accent};
    border-radius: 999px;
    padding: 6px 20px;
    letter-spacing: 0.5px;
    transition: background 0.6s ease, color 0.6s ease;
  }

  .lang-badge.filled {
    background: ${t.accent};
    color: ${isDark ? '#fff' : '#fff'};
  }

  .lang-num {
    font-size: 17px;
    font-weight: 600;
    color: ${t.accent};
    letter-spacing: 2px;
    min-width: 28px;
  }

  .lang-sub {
    font-size: clamp(24px, 3.5vw, 34px);
    font-weight: 300;
    color: ${t.textSub};
    letter-spacing: -0.3px;
  }

  .lang-hl {
    transition: color 0.6s ease, font-weight 0.6s ease;
  }

  .lang-hl.active {
    color: ${t.accent};
    font-weight: 700;
  }

  @keyframes lang-fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }
</style>

<div class="lang-stage">
  <div class="lang-title">
    어떻게 <em>나만의 언어</em>를<br/>
    정의했고, 찾아 나갔는가
  </div>

  <div class="lang-sections">
    <div class="lang-section">
      <div class="lang-header">
        <span class="lang-num">01</span>
        <span class="lang-sub">다른 사람의 언어를 이해하기</span>
      </div>
      <div class="lang-desc">더 높은 곳의 시야를 미리 경험해보기</div>
      <div class="lang-badges">
        <span class="lang-badge">책</span>
        <span class="lang-badge">리뷰어</span>
        <span class="lang-badge">코치</span>
        <span class="lang-badge">크루</span>
      </div>
    </div>
    <div class="lang-section">
      <div class="lang-header">
        <span class="lang-num">02</span>
        <span class="lang-sub">다르게 해석하기</span>
      </div>
      <div class="lang-desc">Ex) 묻지 말고 시켜라.<br/><span class="lang-hl">단어에 내 경험을 붙이기</span><br/>추상화는 같아도, 구체화는 달라야 한다.</div>
    </div>
    <div class="lang-section">
      <div class="lang-header">
        <span class="lang-num">03</span>
        <span class="lang-sub">다시 추상화시키기</span>
      </div>
      <div class="lang-desc">내 경험만으로 <span class="lang-hl">다른 사람이 추상화시켰던 개념에 다시 도달하는 것</span></div>
    </div>
  </div>
</div>
`;

// Click to reveal: 01 → open canvas → 02 → 03
const steps = [
  () => container.querySelector('.lang-section:nth-child(1)').classList.add('visible'),
  () => container.querySelector('.lang-section:nth-child(1) .lang-desc').classList.add('visible'),
  () => container.querySelector('.lang-section:nth-child(1) .lang-badges').classList.add('visible'),
  () => {
    container.querySelector('.lang-badge').classList.add('filled');
    const file = app.vault.getAbstractFileByPath('🟧 Organize 영구노트/🔵 Project 프로젝트/선수타/링크/자료/오브젝트.canvas');
    if (file) app.workspace.getLeaf('split').openFile(file);
  },
  () => { const badges = container.querySelectorAll('.lang-badge'); badges[0].classList.remove('filled'); badges[1].classList.add('filled'); badges[2].classList.add('filled'); badges[3].classList.add('filled'); },
  () => { container.querySelectorAll('.lang-badge').forEach(b => b.classList.remove('filled')); container.querySelector('.lang-section:nth-child(2)').classList.add('visible'); },
  () => container.querySelector('.lang-section:nth-child(2) .lang-desc').classList.add('visible'),
  () => {
    const file = app.vault.getAbstractFileByPath('🟧 Organize 영구노트/🔵 Project 프로젝트/선수타/링크/자료/블랙잭 설계.canvas');
    if (file) app.workspace.getLeaf('split').openFile(file);
  },
  () => container.querySelector('.lang-section:nth-child(3)').classList.add('visible'),
  () => container.querySelector('.lang-section:nth-child(3) .lang-desc').classList.add('visible'),
  () => {
    const file = app.vault.getAbstractFileByPath('🟧 Organize 영구노트/🔵 Project 프로젝트/선수타/링크/자료/다시추상화시키기.canvas');
    if (file) app.workspace.getLeaf('split').openFile(file);
  },
  () => {
    container.querySelectorAll('.lang-hl').forEach(el => el.classList.add('active'));
  },
  () => {
    const next = app.vault.getAbstractFileByPath('🟧 Organize 영구노트/🔵 Project 프로젝트/선수타/링크/ppt/4. 끗.md');
    if (next) app.workspace.getLeaf(false).openFile(next);
  },
];
let stepIndex = 0;
container.querySelector('.lang-stage').addEventListener('click', () => {
  if (stepIndex < steps.length) {
    steps[stepIndex]();
    stepIndex++;
  }
});
```
