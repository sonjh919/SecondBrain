---
cssclasses:
  - wide
---

```dataviewjs
// -- Theme Detection --
function detectIsDarkMode(){
    const body = document.body;
    if(body?.classList?.contains("theme-dark")) return true;
    if(body?.classList?.contains("theme-light")) return false;
    const computedBg = window.getComputedStyle(document.documentElement).backgroundColor || "";
    const ch = computedBg.match(/\d+(?:\.\d+)?/g);
    if(ch && ch.length >= 3){
        const lum = (Number(ch[0])*299 + Number(ch[1])*587 + Number(ch[2])*114)/1000;
        return lum < 128;
    }
    return true;
}

const isDark = detectIsDarkMode();

const t = isDark ? {
    bg: '#08080e',
    text: '#ffffff',
    textMuted: 'rgba(255,255,255,0.35)',
    textSub: 'rgba(255,255,255,0.55)',
    border: 'rgba(255,255,255,0.06)',
    glow: 'rgba(255,255,255,0.03)',
    accent: 'var(--zos-accent, #6366f1)',
    accentGlow: 'rgba(99,102,241,0.15)',
} : {
    bg: '#fafafa',
    text: '#0f0f0f',
    textMuted: 'rgba(0,0,0,0.3)',
    textSub: 'rgba(0,0,0,0.55)',
    border: 'rgba(0,0,0,0.06)',
    glow: 'rgba(0,0,0,0.02)',
    accent: 'var(--zos-accent, #6366f1)',
    accentGlow: 'rgba(99,102,241,0.08)',
};

const container = this.container;
container.style.cssText = 'padding:0;margin:0;';

container.innerHTML = `
<style>
  .eff-stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 82vh;
    padding: 60px 0;
    font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Pretendard', sans-serif;
    position: relative;
    overflow: hidden;
    user-select: none;
  }

  .eff-stage::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 500px;
    height: 500px;
    border-radius: 50%;
    background: ${t.accentGlow};
    filter: blur(120px);
    pointer-events: none;
    z-index: 0;
  }

  .eff-title {
    font-size: clamp(32px, 5vw, 48px);
    font-weight: 200;
    color: ${t.text};
    text-align: center;
    line-height: 1.5;
    letter-spacing: -0.5px;
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: eff-fadeUp 1.2s ease 0.3s forwards;
    margin-bottom: 64px;
  }

  .eff-title em {
    font-style: normal;
    font-weight: 600;
    background: linear-gradient(135deg, ${t.accent}, ${isDark ? '#a78bfa' : '#818cf8'});
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .eff-sections {
    display: flex;
    flex-direction: column;
    gap: 28px;
    position: relative;
    z-index: 1;
  }

  .eff-section {
    display: flex;
    flex-direction: column;
    gap: 12px;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.8s ease, transform 0.8s ease;
    cursor: default;
  }

  .eff-section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .eff-header {
    display: flex;
    align-items: baseline;
    gap: 20px;
  }

  .eff-desc {
    font-size: clamp(17px, 2.5vw, 22px);
    font-weight: 300;
    color: ${t.textMuted};
    line-height: 1.7;
    padding-left: 48px;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }

  .eff-desc.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .eff-num {
    font-size: 17px;
    font-weight: 600;
    color: ${t.accent};
    letter-spacing: 2px;
    min-width: 28px;
  }

  .eff-sub {
    font-size: clamp(24px, 3.5vw, 34px);
    font-weight: 300;
    color: ${t.textSub};
    letter-spacing: -0.3px;
  }

  @keyframes eff-fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }
</style>

<div class="eff-stage">
  <div class="eff-title">
    <em>나만의 언어</em>를 만들면<br/>
    뭐가 달라지나요?
  </div>

  <div class="eff-sections">
    <div class="eff-section">
      <div class="eff-header">
        <span class="eff-num">01</span>
        <span class="eff-sub">외부에 의해 흔들리지 않는다</span>
      </div>
      <div class="eff-desc">AI라는 변화 속에서도, 잡아먹히지 않을까보다 내 색깔을 어떻게 표현할지에 집중하게 된다.</div>
    </div>
    <div class="eff-section">
      <div class="eff-header">
        <span class="eff-num">02</span>
        <span class="eff-sub">잘 받아들인다</span>
      </div>
      <div class="eff-desc">다른 사람의 결정을 이해하고 존중할 수 있게 된다.<br/>내 언어를 이해한 만큼, 다른 사람의 언어도 이해할 수 있게 된다.</div>
    </div>
  </div>
</div>
`;

// Click to reveal: 01 title → 01 desc → 02 title → next page
const steps = [
  () => container.querySelector('.eff-section:nth-child(1)').classList.add('visible'),
  () => container.querySelector('.eff-section:nth-child(1) .eff-desc').classList.add('visible'),
  () => container.querySelector('.eff-section:nth-child(2)').classList.add('visible'),
  () => container.querySelector('.eff-section:nth-child(2) .eff-desc').classList.add('visible'),
  () => {
    const next = app.vault.getAbstractFileByPath('🟧 Organize 영구노트/🔵 Project 프로젝트/선수타/링크/ppt/3. 나만의 언어.md');
    if (next) app.workspace.getLeaf(false).openFile(next);
  }
];
let stepIndex = 0;
container.querySelector('.eff-stage').addEventListener('click', () => {
  if (stepIndex < steps.length) {
    steps[stepIndex]();
    stepIndex++;
  }
});
```
