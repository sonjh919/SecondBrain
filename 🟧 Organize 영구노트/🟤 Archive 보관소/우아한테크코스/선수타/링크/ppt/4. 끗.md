---
cssclasses:
  - wide
---

```dataviewjs
// -- Theme Detection --
function detectIsDarkMode(){
    const body = document.body;
    if(body?.classList?.contains("theme-dark")) return true;
    if(body?.classList?.contains("theme-light")) return false;
    const computedBg = window.getComputedStyle(document.documentElement).backgroundColor || "";
    const ch = computedBg.match(/\d+(?:\.\d+)?/g);
    if(ch && ch.length >= 3){
        const lum = (Number(ch[0])*299 + Number(ch[1])*587 + Number(ch[2])*114)/1000;
        return lum < 128;
    }
    return true;
}

const isDark = detectIsDarkMode();

const t = isDark ? {
    text: '#ffffff',
    accent: 'var(--zos-accent, #6366f1)',
    accentGlow: 'rgba(99,102,241,0.15)',
} : {
    text: '#0f0f0f',
    accent: 'var(--zos-accent, #6366f1)',
    accentGlow: 'rgba(99,102,241,0.08)',
};

const container = this.container;
container.style.cssText = 'padding:0;margin:0;';

container.innerHTML = `
<style>
  .end-stage {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 82vh;
    font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Pretendard', sans-serif;
    position: relative;
    overflow: hidden;
    user-select: none;
  }

  .end-stage::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 500px;
    height: 500px;
    border-radius: 50%;
    background: ${t.accentGlow};
    filter: blur(120px);
    pointer-events: none;
    z-index: 0;
  }

  .end-text {
    font-size: clamp(48px, 8vw, 80px);
    font-weight: 600;
    color: ${t.text};
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: end-fadeUp 1.2s ease 0.3s forwards;
  }

  .end-msg {
    font-size: clamp(16px, 2.5vw, 22px);
    font-weight: 300;
    color: ${t.textMuted};
    text-align: center;
    position: absolute;
    bottom: 15%;
    width: 100%;;
    opacity: 0;
    animation: end-fadeUp 1.2s ease 0.8s forwards;
  }

  @keyframes end-fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }
</style>

<div class="end-stage">
  <div class="end-text">끗</div>
  <div class="end-msg">여러분 각자만의 언어를, 그리고 그것을 찾아가는 방법까지 발견하시길 바랍니다.</div>
</div>
`;
```
