---
tags: [infrastructure, security, reliability, resilience]
created: 2025-10-23
---

# 보안과 안정성

## 1만 명 단계
### 기본 보안

#### Spring Security 설정
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf().csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
            .and()
            .headers()
                .xssProtection()
                .and()
                .contentSecurityPolicy("default-src 'self'")
            .and()
            .authorizeHttpRequests()
                .requestMatchers("/api/public/**").permitAll()
                .anyRequest().authenticated();
        return http.build();
    }
}
```

#### Rate Limiting (Bucket4j)
```java
@Component
public class RateLimitInterceptor implements HandlerInterceptor {
    private final Map<String, Bucket> cache = new ConcurrentHashMap<>();
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                            HttpServletResponse response, 
                            Object handler) {
        String key = getClientIP(request);
        Bucket bucket = resolveBucket(key);
        
        if (bucket.tryConsume(1)) {
            return true;
        } else {
            response.setStatus(429);
            return false;
        }
    }
    
    private Bucket resolveBucket(String key) {
        return cache.computeIfAbsent(key, k -> 
            Bucket.builder()
                .addLimit(Bandwidth.simple(100, Duration.ofMinutes(1)))
                .build()
        );
    }
}
```

---

## 10만 명 단계
### Circuit Breaker & 장애 격리

#### Resilience4j 설정
```yaml
resilience4j:
  circuitbreaker:
    instances:
      userService:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3
  retry:
    instances:
      userService:
        maxAttempts: 3
        waitDuration: 1s
```

```java
@Service
public class RoomService {
    @CircuitBreaker(name = "userService", fallbackMethod = "fallbackGetUser")
    @Retry(name = "userService")
    public UserDto getUser(Long userId) {
        return userServiceClient.getUser(userId);
    }
    
    private UserDto fallbackGetUser(Long userId, Exception e) {
        log.error("Failed to get user, returning default", e);
        return UserDto.createDefault(userId);
    }
}
```

#### API Gateway Rate Limiting
```yaml
# Spring Cloud Gateway
spring:
  cloud:
    gateway:
      routes:
        - id: room-service
          uri: lb://room-service
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 100
                redis-rate-limiter.burstCapacity: 200
                redis-rate-limiter.requestedTokens: 1
```

#### 데이터 암호화
```java
// 민감 정보 암호화
@Component
public class EncryptionService {
    @Autowired
    private TextEncryptor textEncryptor;
    
    public String encrypt(String plainText) {
        return textEncryptor.encrypt(plainText);
    }
    
    public String decrypt(String encrypted) {
        return textEncryptor.decrypt(encrypted);
    }
}

// JPA Converter
@Converter
public class EmailEncryptConverter implements AttributeConverter<String, String> {
    @Autowired
    private EncryptionService encryptionService;
    
    @Override
    public String convertToDatabaseColumn(String email) {
        return encryptionService.encrypt(email);
    }
    
    @Override
    public String convertToEntityAttribute(String encrypted) {
        return encryptionService.decrypt(encrypted);
    }
}
```

---

## 1000만 명 단계
### 종합 보안 아키텍처

#### WAF (Web Application Firewall)
- AWS WAF 또는 Cloudflare
- SQL Injection, XSS 방어
- DDoS 방어

```yaml
# AWS WAF 규칙
Rules:
  - SQLInjectionRule: BLOCK
  - XSSRule: BLOCK
  - RateLimitRule: 
      Limit: 2000 requests per 5 minutes
  - GeoBlockingRule:
      BlockedCountries: ["CN", "RU"]  # 필요시
```

#### Zero Trust Architecture
```java
// JWT + mTLS
@Configuration
public class SecurityConfig {
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .oauth2ResourceServer()
                .jwt()
            .and()
            .requiresChannel()
                .anyRequest().requiresSecure()  // HTTPS 강제
            .and()
            .x509()
                .subjectPrincipalRegex("CN=(.*?)(?:,|$)")  // mTLS
                .userDetailsService(userDetailsService());
        return http.build();
    }
}
```

#### Secrets Management
```yaml
# AWS Secrets Manager 연동
spring:
  cloud:
    aws:
      secretsmanager:
        enabled: true
        prefix: /secret/prod

# 또는 HashiCorp Vault
spring:
  cloud:
    vault:
      uri: https://vault.example.com
      authentication: TOKEN
```

```java
@Configuration
public class DatabaseConfig {
    @Value("${spring.cloud.aws.secretsmanager.db-password}")
    private String dbPassword;
    
    @Bean
    public DataSource dataSource() {
        // dbPassword는 런타임에 Secrets Manager에서 가져옴
    }
}
```

#### 감사 로그 (Audit Log)
```java
@Aspect
@Component
public class AuditAspect {
    @Autowired
    private AuditLogRepository auditLogRepository;
    
    @AfterReturning(
        pointcut = "@annotation(auditable)",
        returning = "result"
    )
    public void logAudit(JoinPoint joinPoint, Auditable auditable, Object result) {
        AuditLog log = AuditLog.builder()
            .userId(SecurityContextHolder.getContext().getAuthentication().getName())
            .action(auditable.action())
            .resource(joinPoint.getSignature().getName())
            .timestamp(Instant.now())
            .ipAddress(RequestContextHolder.currentRequestAttributes()...)
            .build();
            
        auditLogRepository.save(log);
    }
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.METHOD)
public @interface Auditable {
    String action();
}

// 사용 예시
@Auditable(action = "DELETE_ROOM")
public void deleteRoom(Long roomId) {
    roomRepository.deleteById(roomId);
}
```

#### 재해 복구 (Disaster Recovery)
```yaml
# Multi-Region 백업
Backup Strategy:
  - RTO (Recovery Time Objective): 1 hour
  - RPO (Recovery Point Objective): 5 minutes
  
Database:
  - Primary: ap-northeast-2 (Seoul)
  - Replica: ap-northeast-1 (Tokyo)
  - Backup: S3 Cross-Region Replication
  
Application:
  - Blue-Green Deployment
  - Canary Release (10% → 50% → 100%)
```

#### 컴플라이언스
- **GDPR**: 개인정보 삭제 요청 처리
- **개인정보보호법**: 암호화, 접근 제어
- **PCI-DSS**: 결제 정보 보안 (결제 기능 있을 경우)

```java
// GDPR 개인정보 삭제
@Service
public class GdprService {
    @Transactional
    public void deleteUserData(Long userId) {
        // 1. 사용자 데이터 삭제
        userRepository.deleteById(userId);
        
        // 2. 관련 데이터 익명화
        roomRepository.anonymizeUserData(userId);
        commentRepository.anonymizeUserData(userId);
        
        // 3. 감사 로그 기록
        auditLogRepository.save(new AuditLog("USER_DATA_DELETED", userId));
    }
}
```

#### Penetration Testing
- 정기적인 침투 테스트 (연 2회)
- Bug Bounty 프로그램 운영
- 보안 취약점 스캔 자동화

---

## 핵심 포인트
- 1만: Spring Security + Rate Limiting
- 10만: Circuit Breaker + API Gateway + 암호화
- 1000만: WAF + Zero Trust + Secrets Manager + DR
